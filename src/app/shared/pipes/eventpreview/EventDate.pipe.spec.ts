import { EventDatePipe } from './EventDate.pipe';

describe('DateforeventpreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new EventDatePipe();
    expect(pipe).toBeTruthy();
  });
});
